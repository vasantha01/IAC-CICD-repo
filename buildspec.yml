version: 0.2
env:
    git-credential-helper: yes 
phases:  
  build:
    commands: | 
        git version
        aws s3 cp cfn-nested/* s3://code-build-input-bucket1/
        aws codepipeline start-pipeline-execution --name Iac-pipeline
        echo "ci-cd" 
        
